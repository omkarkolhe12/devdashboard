apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-pv
  labels:
    app: devdashboard-postgres
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  hostPath:
    path: /mnt/devdashboard-postgres
