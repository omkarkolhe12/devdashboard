apiVersion: v1
kind: Service
metadata:
  name: devdashboard-frontend-service
  namespace: devdahsboard
spec:
  selector:
    app.kubernetes.io/name: devdashboard-frontend
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 4173
    nodePort: 30080
  type: NodePort

